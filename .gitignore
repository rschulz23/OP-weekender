# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# Playwright browser binaries
playwright-browsers/

# Local scratch output — CI commits these, local runs are ignored
# (Git will track them once CI commits; local regenerations won't show as dirty)
newsletter.html
events.json

# Secrets / env
.env
*.env

# macOS
.DS_Store
